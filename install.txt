sudo apt install virtualenv

clone no repo
entra no repo

virtualenv -p python3 env
sudo apt-get install python-mysqldb
sudo apt-get install libmysqlclient-dev

source env/bin/activate

pip install -r /path/to/requiriments.txt

